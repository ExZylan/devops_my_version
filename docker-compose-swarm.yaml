version: '3.9'

services:
  covid19_1:
    image: zylan/repositoriozylanv2:v1
    build: .
    ports:
      - "40001:8000"
    deploy:
      replicas: 3
      update_config:
        delay: 10s

  redis:
    image: redis:alpine